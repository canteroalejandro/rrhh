h1 Listing check_ins

table class="table table-striped table-bordered table-hover"
  thead
    tr
      th Usuario que realizó la acción
      th Empleado
      th Tardanza
      / th Días que trabaja
      / th Hora en que trabaja
      th Día y hora de entrada:
      th colspan="3" Acciones

  tbody
    - @check_ins.each do |check_in|
      tr
        td = @current_user.userName
        td = check_in.empleado.nombre_y_apellido
        td
          -if check_in.tardanza == true
            = 'Sí'
          -else
            = 'No'
        / td
        /   - check_in.empleado.horario_empleado do |hor_emp|
        /     = hor_emp.id
          /  = hor_emp.horario.string_por_dias(horario.dia)
        / td = "#{check_in.horario_empleado.horario.horaEntrada.strftime("%H:%M %P")} - #{check_in.horario_empleado.horario.horaSalida.strftime("%H:%M %P")}"
        td = check_in.horaOutput.strftime("%a, %e de %B de %Y, %H:%M hs")
        td = link_to 'Show', check_in
        td = link_to 'Edit', edit_check_in_path(check_in)
        td = link_to 'Destroy', check_in, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Check in', new_check_in_path
