h1 Listing incidencia_empleados

table class="table table-striped table-bordered table-hover"
  thead
    tr
      th Empleado
      th Descripción de Incidencia
      th Fecha de Inicio
      th Fecha en que Termina
      th colspan="3" Acciones

  tbody
    - @incidencia_empleados.each do |incidencia_empleado|
      tr
        td = incidencia_empleado.empleado.nombre_y_apellido
        td = incidencia_empleado.incidencia.nombre
        td = incidencia_empleado.inicio
        td = incidencia_empleado.fin
        td = link_to 'Show', [incidencia_empleado.empleado, incidencia_empleado]
        td = link_to 'Edit', edit_empleado_incidencia_empleado_path(incidencia_empleado.empleado, incidencia_empleado)
        td = link_to 'Destroy', [incidencia_empleado.empleado, incidencia_empleado], data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to 'New Incidencia Empleado', new_empleado_incidencia_empleado_path
